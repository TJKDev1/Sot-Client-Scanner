[project]
name = "sot-client-scanner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mitmproxy>=12.2.1",
    "pyside6>=6.10.2",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.19.0",
]
